#include<stdio.h>
#include<math.h>
#include<string.h>

int main(){
	char a[1000];
	scanf("%s",a);
	int x=strlen(a);
	char max=a[0];
	for(int i=0; i<x; i++){
		max=fmax(max,a[i]);
	}
	printf("%c", max);
	return 0;
}
